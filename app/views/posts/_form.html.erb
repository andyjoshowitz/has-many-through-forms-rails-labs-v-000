<%= form_for(@post) do |f| %>
  <p>
    <%= f.label :content %>
    <%= f.xt_field :content %>
  </p>

  <p>
    <%= f.label :content %>
    <%= f.xt_field :content %>
  </p>

  <% Category.each do |c| %>
    <p>
      <label for="category_id_1 <%= c.id %>"><%= c.name %></label>
      <input type="checkbox" name="post[category_ids]" value="1">
    </p>
  <% end %>
  <%= f.submit %>
<%= end %>
